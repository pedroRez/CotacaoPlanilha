import React from 'react';
import logo from '../../logo.svg';

export default props => (
  <header className="App-header">
    <img src={logo} className="App-logo" alt="logo" />
    <h1 className="App-title">Sistema de Cotação por planilhas</h1>
  </header>
)
